export const RUNTIME_DELEGATION_EXECUTION_CONTRACT = [
  "You are already the AOP-selected runtime.",
  "Treat runtime-selection wording in the request as orchestration metadata, not as an instruction to delegate again.",
  "Perform the underlying task directly. Do not look for or launch another runtime, CLI, or AOP worker to handle it. AOP workers are a separate platform concept.",
].join(" ");
